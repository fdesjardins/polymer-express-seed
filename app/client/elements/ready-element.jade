link(rel="import", href="../bower_components/polymer/polymer.html")
  
dom-module(id="ready-element")
  template
    | This element has a ready() method.
    span(id="el") Not ready...
    
  script
    :coffee
      Polymer
        is: 'ready-element'
        properties:
          type: String
          owner: 'Daniel'
        ready: ->
          @$.el.textContent = "#{@properties.owner} is ready!"
    
