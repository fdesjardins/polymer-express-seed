link(rel="import", href="../bower_components/polymer/polymer.html")
  
polymer-element(name="ready-element")
  template
    | This element has a ready() method.
    span(id="el") Not ready...
    
  script
    :coffee
      Polymer
        owner: 'Daniel'
        ready: ->
          @$.el.textContent = "#{@owner} is ready!"
    
